function bindDragDropUpload(containerId,sendFileFn){const box=document.getElementById(containerId);if(!box||box.dataset.dragBound)return;box.dataset.dragBound="1";let dragCounter=0;box.addEventListener("dragenter",e=>{e.preventDefault(),dragCounter++,box.classList.add("drag-over")}),box.addEventListener("dragover",e=>{e.preventDefault()}),box.addEventListener("dragleave",e=>{e.preventDefault(),dragCounter--,dragCounter<=0&&(dragCounter=0,box.classList.remove("drag-over"))}),box.addEventListener("drop",e=>{e.preventDefault(),dragCounter=0,box.classList.remove("drag-over");const files=e.dataTransfer?.files;if(!files||!files.length)return;const fakeInput={files:[files[0]],value:""};sendFileFn(fakeInput)})}function bindRoomInfiniteScroll(){const box=document.getElementById("chatMessages");box&&!box.dataset.infiniteBound&&(box.dataset.infiniteBound="1",box.addEventListener("scroll",()=>{box.scrollTop<80&&loadOlderRoomMessages(),updateRoomScrollDownBtn()}))}let roomUnseenWhileScrolledUp=0;function updateRoomScrollDownBtn(){const box=document.getElementById("chatMessages"),btn=document.getElementById("roomScrollDownBtn");if(!box||!btn)return;const isNearBottom=box.scrollHeight-box.scrollTop-box.clientHeight<150;btn.classList.toggle("visible",!isNearBottom),isNearBottom&&(roomUnseenWhileScrolledUp=0,document.getElementById("roomScrollDownBadge").style.display="none")}function scrollRoomToBottom(){const box=document.getElementById("chatMessages");box&&(box.scrollTo({top:box.scrollHeight,behavior:"smooth"}),roomUnseenWhileScrolledUp=0,document.getElementById("roomScrollDownBadge").style.display="none")}async function loadOlderRoomMessages(){if(loadingOlderRoomMessages||!roomHasMoreOlder||!currentRoomMessages.length||!activeRoomId)return;loadingOlderRoomMessages=!0;const box=document.getElementById("chatMessages"),loadingEl=document.createElement("div");loadingEl.className="older-msgs-loading",loadingEl.innerHTML='<div class="spinner"></div>',box.prepend(loadingEl);const oldestCreatedAt=currentRoomMessages[0].created_at,{data:olderDesc}=await sb.from("chat_messages").select("*, profiles!sender_id(username, avatar_file_id), reply:reply_to_id(id, sender_id, content, profiles!sender_id(username))").eq("room_id",activeRoomId).lt("created_at",oldestCreatedAt).order("created_at",{ascending:!1}).limit(100);loadingEl.remove();const older=(olderDesc||[]).slice().reverse();if(!older.length)return roomHasMoreOlder=!1,void(loadingOlderRoomMessages=!1);older.length<100&&(roomHasMoreOlder=!1),currentRoomMessages=[...older,...currentRoomMessages],cacheMessages(conversationIdFor("room",activeRoomId),older);const prevScrollHeight=box.scrollHeight,prevScrollTop=box.scrollTop;prependMessages(box,older),box.scrollTop=box.scrollHeight-prevScrollHeight+prevScrollTop,loadingOlderRoomMessages=!1}function prependMessages(box,older){const fragment=older.map(m=>renderOneMessage(m,m.profiles?.username,m.profiles?.avatar_file_id)).join("");box.insertAdjacentHTML("afterbegin",fragment),older.forEach(m=>{m.profiles?.avatar_file_id&&loadAvatarInto(`msgav-${m.id}`,m.profiles.avatar_file_id),!m.is_removed&&m.telegram_file_id&&loadMessageMedia(m),m.is_removed||loadMessageReactions(m.id)}),applyLocalNicknamesToSenders(box)}async function applyLocalNicknamesToSenders(container){const senderEls=container.querySelectorAll(".sender[data-sender-id]"),uniqueIds=[...new Set([...senderEls].map(el=>el.dataset.senderId))];for(const uid of uniqueIds){const nick=await getNickname(uid);nick&&container.querySelectorAll(`.sender[data-sender-id="${uid}"]`).forEach(el=>{el.textContent="@"+nick,el.title="اسم مستعار محلي"})}}function fromCachedMessage(c){return{id:c.id,sender_id:c.sender_id,room_id:c.room_id,receiver_id:c.receiver_id,content:c.content,message_type:c.message_type,telegram_file_id:c.telegram_file_id,duration_seconds:c.duration_seconds,is_removed:c.is_removed,is_pinned:c.is_pinned,created_at:c.created_at,edited_at:c.edited_at,reply_to_id:c.reply_to_id,reply:null,profiles:c.sender_username?{username:c.sender_username}:null}}function findFirstUnreadMessageId(msgs,lastReadAt){if(!lastReadAt)return null;const cutoff=new Date(lastReadAt).getTime(),firstUnread=msgs.find(m=>m.sender_id!==currentUser.id&&new Date(m.created_at).getTime()>cutoff);return firstUnread?firstUnread.id:null}function canModerateRoom(){return["owner","super_admin","admin","moderator"].includes(currentProfile.role)||activeRoomCreatorId&&activeRoomCreatorId===currentUser.id}function canDeleteMessage(m){return m.sender_id===currentUser.id||["owner","super_admin","admin","moderator"].includes(currentProfile.role)||activeRoomCreatorId&&activeRoomCreatorId===currentUser.id}function renderMessages(msgs,firstUnreadId){const box=document.getElementById("chatMessages");if(box){if(box.innerHTML=msgs.map(m=>(firstUnreadId&&m.id===firstUnreadId?'<div class="unread-divider">رسائل غير مقروءة</div>':"")+renderOneMessage(m,m.profiles?.username,m.profiles?.avatar_file_id)).join(""),bindMsgLongPress(box),msgs.forEach(m=>{m.profiles?.avatar_file_id&&loadAvatarInto(`msgav-${m.id}`,m.profiles.avatar_file_id),!m.is_removed&&m.telegram_file_id&&loadMessageMedia(m),m.is_removed||loadMessageReactions(m.id)}),applyLocalNicknamesToSenders(box),firstUnreadId){const dividerEl=box.querySelector(".unread-divider");if(dividerEl)return dividerEl.scrollIntoView({block:"center"}),void updateRoomScrollDownBtn()}box.scrollTop=box.scrollHeight,updateRoomScrollDownBtn()}}function renderOneMessage(m,username,avatarFileId){const mine=m.sender_id===currentUser.id,initials=(username||"?")[0]?.toUpperCase()||"?",usernameEsc=escapeHtml(username||"").replace(/'/g,"\\'"),isMedia=!m.is_removed&&("image"===m.message_type||"video"===m.message_type);return`\n    <div class="msg-row ${mine?"mine":""}" data-msg-id="${m.id}">\n      <div class="msg-avatar" id="msgav-${m.id}" onclick="viewProfile('${m.sender_id}')">${initials}</div>\n      <i class="fa-solid fa-reply msg-reply-hint"></i>\n      <div style="display:flex;flex-direction:column;max-width:66%">\n        <div class="msg ${mine?"mine":""} ${isMedia?"has-media":""}" id="msgel-${m.id}" style="position:relative;max-width:100%" data-msg-id="${m.id}" data-ctx="room" data-username="${usernameEsc}">\n          ${m.is_removed?"":`<div class="sender" id="sendername-${m.id}" data-sender-id="${m.sender_id}" style="cursor:pointer" onclick="event.stopPropagation();viewProfile('${m.sender_id}')">@${username||""}</div>`}\n          ${m.is_removed?'<i style="color:var(--ink-faint)">تم حذف هذه الرسالة</i>':`\n            ${m.reply?`<div class="msg-quote-ref" style="border-right-color:${colorForUser(m.reply.sender_id)}" onclick="event.stopPropagation();scrollToMessage('${m.reply.id||""}')"><i class="fa-solid fa-arrow-turn-up msg-quote-arrow" style="color:${colorForUser(m.reply.sender_id)}"></i><span style="color:${colorForUser(m.reply.sender_id)};font-weight:700">@${m.reply.profiles?.username||""}</span>: ${renderMentionText(escapeHtml((m.reply.content||"").slice(0,50)))}</div>`:""}\n            <div class="no-select" style="position:relative">${renderMessageBody(m)}${isMedia?`<span class="msg-media-time-overlay">${formatMsgTime(m.created_at)}${m.edited_at?" · معدّلة":""}</span>`:""}</div>\n            ${isMedia?"":`<div class="msg-timestamp-row" style="font-size:10px;color:${mine?"rgba(255,255,255,.7)":"var(--ink-faint)"};margin-top:3px;text-align:left">${formatMsgTime(m.created_at)}${m.edited_at?" · معدّلة":""}${m.is_pinned?' · <i class="fa-solid fa-thumbtack"></i>':""}</div>`}\n          `}\n        </div>\n        <div class="msg-reaction-row ${mine?"mine":""}" id="msgreact-${m.id}"></div>\n      </div>\n    </div>`}async function openMessageInfo(msgId){document.getElementById("messageInfoOverlay")?.classList.add("active");const listEl=document.getElementById("messageInfoList");listEl&&(listEl.innerHTML='<div class="empty-state"><div class="spinner"></div></div>');const{data:readers,error:error}=await sb.rpc("get_message_read_by",{p_message_id:msgId});listEl&&(error?listEl.innerHTML=`<div class="empty-state" style="color:var(--red)">تعذر التحميل: ${escapeHtml(error.message)}</div>`:readers?.length?(listEl.innerHTML=readers.map((r,i)=>`\n    <div class="member-row" style="animation:fadeInUp 0.3s var(--ease-out) ${.04*i}s both">\n      <div class="avatar" id="msginfo-av-${r.user_id}" style="width:28px;height:28px;font-size:12px;cursor:pointer" onclick="closeMessageInfo();viewProfile('${r.user_id}')">${(r.username||"?")[0].toUpperCase()}</div>\n      <div class="name" style="cursor:pointer;flex:1" onclick="closeMessageInfo();viewProfile('${r.user_id}')">${escapeHtml(r.full_name||r.username||"مستخدم")}</div>\n      <div class="meta" style="font-size:11px;color:var(--ink-faint)">${formatMsgTime(r.read_at)}</div>\n    </div>`).join(""),readers.forEach(r=>{r.avatar_file_id&&loadAvatarInto(`msginfo-av-${r.user_id}`,r.avatar_file_id)})):listEl.innerHTML='<div class="empty-state">محدش شاف الرسالة دي لسه</div>')}function closeMessageInfo(){document.getElementById("messageInfoOverlay")?.classList.remove("active")}function renderMessageBody(m){return"voice"===m.message_type?`<div class="msg-voice" id="msgmedia-${m.id}"><i class="fa-solid fa-microphone"></i> <span style="font-size:12px;color:var(--ink-faint)">${m.duration_seconds?formatDuration(m.duration_seconds):""}</span></div>`:"image"===m.message_type||"video"===m.message_type?`<div class="msg-media" id="msgmedia-${m.id}">${escapeHtml(m.content||"")}</div>`:"document"===m.message_type?`<div id="msgmedia-${m.id}"><i class="fa-solid fa-paperclip"></i> ${escapeHtml(m.content||"ملف")}</div>`:renderMentionText(escapeHtml(m.content||""))}function renderMentionText(escapedText){return escapedText?escapedText.replace(/@([A-Za-z0-9_\u0600-\u06FF]+)/g,'<span class="mention-tag">@$1</span>'):escapedText}function scrollToMessage(msgId){if(!msgId)return;const el=document.getElementById(`msgel-${msgId}`);el?(el.scrollIntoView({behavior:"smooth",block:"center"}),el.classList.add("msg-flash-highlight"),setTimeout(()=>el.classList.remove("msg-flash-highlight"),1600)):toast("الرسالة مش محمّلة في الشاشة دلوقتي","error")}function formatDuration(sec){return`${Math.floor(sec/60)}:${String(sec%60).padStart(2,"0")}`}async function loadMessageMedia(m){const el=document.getElementById(`msgmedia-${m.id}`);if(!el)return;if("video"===m.message_type)return void(el.innerHTML=`<div class="video-play-placeholder" style="height:160px" onclick="playStreamedMessageVideo('${m.id}', '${escapeHtml(m.telegram_file_id)}')">\n      <i class="fa-solid fa-circle-play"></i>\n    </div>`);if("voice"===m.message_type){const streamUrl=await getTelegramStreamUrl(m.telegram_file_id);if(!streamUrl)return;return void(el.innerHTML=`<audio controls src="${streamUrl}"></audio> <span style="font-size:12px;color:var(--ink-faint)">${m.duration_seconds?formatDuration(m.duration_seconds):""}</span>`)}const cached=getCachedFile(m.telegram_file_id),url=cached||await getTelegramFileBlobUrl(m.telegram_file_id);if(url){if(!cached)try{const res=await fetch(url),blob=await res.blob(),dataUrl=await blobToDataURL(blob);setCachedFile(m.telegram_file_id,dataUrl)}catch(e){}el.innerHTML=`<img src="${url}" onclick="event.stopPropagation();openImageViewer('${url}')">`}}async function playStreamedMessageVideo(msgId,fileId){const el=document.getElementById(`msgmedia-${msgId}`);if(!el)return;const streamUrl=await getTelegramStreamUrl(fileId);streamUrl?el.innerHTML=`<video src="${streamUrl}" controls playsinline onclick="event.stopPropagation();openVideoViewer('${streamUrl}')"></video>`:toast("تعذر تشغيل الفيديو","error")}async function togglePinMessage(msgId,pinTo){const{error:error}=await sb.from("chat_messages").update({is_pinned:pinTo}).eq("id",msgId);error&&toast("خطأ: "+error.message,"error")}async function editMessage(msgId,currentContent){const newContent=await customPrompt("عدّل رسالتك:",currentContent);if(null===newContent||!newContent.trim()||newContent===currentContent)return;const{error:error}=await sb.from("chat_messages").update({content:newContent.trim(),edited_at:(new Date).toISOString()}).eq("id",msgId);error&&toast("تعذر التعديل: "+error.message,"error")}async function deleteMessage(msgId){if(!await customConfirm("تحذف الرسالة دي؟",!0))return;const{error:error}=await sb.from("chat_messages").update({is_removed:!0}).eq("id",msgId);error&&toast("تعذر الحذف: "+error.message,"error")}function setReplyTarget(msgId,username,preview){replyingTo=msgId;const bar=document.getElementById("replyBar");bar&&(bar.style.display="flex",document.getElementById("replyBarText").textContent=`رد على @${username}: ${preview}`)}function cancelReply(){replyingTo=null;const bar=document.getElementById("replyBar");bar&&(bar.style.display="none")}function isScrolledNearBottom(box){return!box||box.scrollHeight-box.scrollTop-box.clientHeight<120}let roomPollInterval=null,lastSeenRoomMsgAt={};function subscribeToRoom(roomId){roomChannel&&sb.removeChannel(roomChannel),roomChannel=sb.channel("room-"+roomId).on("postgres_changes",{event:"INSERT",schema:"public",table:"chat_messages",filter:`room_id=eq.${roomId}`},async payload=>{await appendRoomMessageIfNew(payload.new.id)}).on("postgres_changes",{event:"UPDATE",schema:"public",table:"chat_messages",filter:`room_id=eq.${roomId}`},async payload=>{await patchRoomMessage(payload.new.id,roomId)}).subscribe(),roomPollInterval&&clearInterval(roomPollInterval),roomPollInterval=setInterval(()=>pollRoomMessages(roomId),3e3)}async function appendRoomMessageIfNew(msgId){const box=document.getElementById("chatMessages");if(!box)return;if(document.getElementById(`msgav-${msgId}`))return;const wasNearBottom=isScrolledNearBottom(box),{data:full}=await sb.from("chat_messages").select("*, profiles!sender_id(username, avatar_file_id), reply:reply_to_id(id, sender_id, content, profiles!sender_id(username))").eq("id",msgId).single();full&&(document.getElementById(`msgav-${full.id}`)||(box.insertAdjacentHTML("beforeend",renderOneMessage(full,full.profiles?.username,full.profiles?.avatar_file_id)),full.profiles?.avatar_file_id&&loadAvatarInto(`msgav-${full.id}`,full.profiles.avatar_file_id),full.telegram_file_id&&loadMessageMedia(full),wasNearBottom&&(box.scrollTop=box.scrollHeight),full.room_id&&(lastSeenRoomMsgAt[full.room_id]=full.created_at)))}async function patchRoomMessage(msgId,roomId){const{data:full}=await sb.from("chat_messages").select("*, profiles!sender_id(username, avatar_file_id), reply:reply_to_id(id, sender_id, content, profiles!sender_id(username))").eq("id",msgId).single();if(!full)return;const row=document.getElementById(`msgav-${full.id}`)?.closest(".msg-row");row&&(row.outerHTML=renderOneMessage(full,full.profiles?.username,full.profiles?.avatar_file_id)),full.profiles?.avatar_file_id&&loadAvatarInto(`msgav-${full.id}`,full.profiles.avatar_file_id),!full.is_removed&&full.telegram_file_id&&loadMessageMedia(full),refreshPinnedBanner(roomId)}async function pollRoomMessages(roomId){if(roomId!==activeRoomId)return;const since=lastSeenRoomMsgAt[roomId];if(!since)return;const{data:rows}=await sb.from("chat_messages").select("id, created_at").eq("room_id",roomId).gt("created_at",since).order("created_at",{ascending:!0}).limit(50);if(rows?.length)for(const row of rows)await appendRoomMessageIfNew(row.id)}async function refreshPinnedBanner(roomId){const{data:pinned}=await sb.from("chat_messages").select("id, content").eq("room_id",roomId).eq("is_pinned",!0).eq("is_removed",!1).order("created_at",{ascending:!1}).limit(1),banner=document.getElementById("pinnedBanner");if(!banner)return;if(!pinned?.length)return void(banner.innerHTML="");const last=pinned[0];banner.innerHTML=`<div style="padding:8px 20px;background:rgba(99,102,241,.08);border-bottom:1px solid var(--border);font-size:12px;display:flex;align-items:center;gap:8px">\n    <span><i class="fa-solid fa-thumbtack"></i></span><span style="flex:1">${escapeHtml((last.content||"").slice(0,80))}</span>\n    <span style="cursor:pointer;color:var(--ink-faint)" onclick="togglePinMessage('${last.id}', false)">إلغاء التثبيت</span>\n  </div>`}let typingChannel=null,typingTimeout=null,typingUsers=new Map;function subscribeToTyping(roomId){typingChannel&&sb.removeChannel(typingChannel),typingUsers=new Map,typingChannel=sb.channel("typing-"+roomId).on("broadcast",{event:"typing"},payload=>{const{user_id:user_id,username:username}=payload.payload;if(user_id===currentUser.id)return;typingUsers.set(user_id,username),renderTypingIndicator(),clearTimeout(typingUsers.get("_timeout_"+user_id));const t=setTimeout(()=>{typingUsers.delete(user_id),renderTypingIndicator()},3e3);typingUsers.set("_timeout_"+user_id,t)}).subscribe()}function renderTypingIndicator(){const el=document.getElementById("typingIndicator");if(!el)return;const names=[...typingUsers.entries()].filter(([k])=>!k.startsWith("_timeout_")).map(([,v])=>v);if(!names.length)return el.innerHTML="",void el.classList.remove("typing-indicator-active");el.classList.add("typing-indicator-active"),el.innerHTML=`${escapeHtml(names.join("، "))} بيكتب<span class="typing-dots"><span></span><span></span><span></span></span>`}function broadcastTyping(){typingChannel&&activeRoomId&&(clearTimeout(typingTimeout),typingChannel.send({type:"broadcast",event:"typing",payload:{user_id:currentUser.id,username:currentProfile.username}}))}let sendingChatMessage=!1;async function sendChatMessage(){if(sendingChatMessage)return;const input=document.getElementById("chatInput"),content=input.value.trim();if(!content||!activeRoomId)return;sendingChatMessage=!0,input.disabled=!0;const{error:error}=await sb.from("chat_messages").insert({room_id:activeRoomId,sender_id:currentUser.id,content:content,message_type:"text",reply_to_id:replyingTo});input.disabled=!1,sendingChatMessage=!1,error?toast("تعذر الإرسال: "+error.message,"error"):(input.value="",input.style.height="auto",input.focus(),cancelReply())}async function sendChatFile(input){const file=input.files[0];if(file&&activeRoomId)try{const kind=file.type.startsWith("image")?"photo":file.type.startsWith("video")?"video":"document",up=await uploadToTelegram(file,kind,{type:"room",roomId:activeRoomId}),{error:error}=await sb.from("chat_messages").insert({room_id:activeRoomId,sender_id:currentUser.id,message_type:"photo"===kind?"image":kind,telegram_file_id:up.file_id,telegram_message_id:up.telegram_message_id||null,content:file.name});if(error)throw error;input.value=""}catch(e){toast("فشل رفع الملف: "+e.message,"error")}}let voiceRecorder=null,voiceChunks=[],voiceStartTime=0,voiceTimerInterval=null,voiceStream=null;async function startVoiceRecording(context){if(voiceRecorder&&"recording"===voiceRecorder.state)return;if(!navigator.mediaDevices||!window.MediaRecorder)return void toast("تسجيل الصوت مش مدعوم في المتصفح ده","error");try{voiceStream=await navigator.mediaDevices.getUserMedia({audio:!0})}catch(e){return void toast("محتاجين إذن الميكروفون عشان تبعت رسالة صوتية","error")}voiceChunks=[],voiceRecorder=new MediaRecorder(voiceStream),voiceRecorder.ondataavailable=e=>{e.data.size>0&&voiceChunks.push(e.data)},voiceRecorder.start(),voiceStartTime=Date.now();const barId="room"===context?"roomRecordBar":"dmRecordBar",timeId="room"===context?"roomRecordTime":"dmRecordTime";document.getElementById(barId)?.classList.add("active"),voiceTimerInterval=setInterval(()=>{const sec=Math.floor((Date.now()-voiceStartTime)/1e3),el=document.getElementById(timeId);el&&(el.textContent=formatDuration(sec))},250)}function cancelVoiceRecording(context){voiceRecorder&&"recording"===voiceRecorder.state&&(voiceRecorder.onstop=null,voiceRecorder.stop()),cleanupVoiceRecording(context)}function cleanupVoiceRecording(context){clearInterval(voiceTimerInterval),voiceTimerInterval=null,voiceStream&&(voiceStream.getTracks().forEach(t=>t.stop()),voiceStream=null);const barId="room"===context?"roomRecordBar":"dmRecordBar";document.getElementById(barId)?.classList.remove("active"),voiceRecorder=null,voiceChunks=[]}async function stopVoiceRecording(context){if(!voiceRecorder||"recording"!==voiceRecorder.state)return void cleanupVoiceRecording(context);const durationSec=Math.round((Date.now()-voiceStartTime)/1e3),finalizedBlob=await new Promise(resolve=>{voiceRecorder.onstop=()=>resolve(new Blob(voiceChunks,{type:voiceRecorder.mimeType||"audio/webm"})),voiceRecorder.stop()});if(cleanupVoiceRecording(context),durationSec<1)return void toast("التسجيل قصير جدًا","error");const file=new File([finalizedBlob],`voice-${Date.now()}.webm`,{type:finalizedBlob.type});try{const uploadContext="room"===context?{type:"room",roomId:activeRoomId}:{type:"dm",otherUserId:activeDmUserId},up=await uploadToTelegram(file,"voice",uploadContext);if("room"===context){if(!activeRoomId)return;const{error:error}=await sb.from("chat_messages").insert({room_id:activeRoomId,sender_id:currentUser.id,message_type:"voice",telegram_file_id:up.file_id,telegram_message_id:up.telegram_message_id||null,duration_seconds:durationSec,reply_to_id:replyingTo});if(error)throw error;cancelReply()}else{if(!activeDmUserId)return;const{error:error}=await sb.from("chat_messages").insert({sender_id:currentUser.id,receiver_id:activeDmUserId,message_type:"voice",telegram_file_id:up.file_id,telegram_message_id:up.telegram_message_id||null,duration_seconds:durationSec,reply_to_id:dmReplyingTo});if(error)throw error;cancelDmReply()}}catch(e){toast("فشل إرسال الرسالة الصوتية: "+e.message,"error")}}async function renderProfile(container,userId){const isOwn=userId===currentUser.id;let p;if(isOwn)p=currentProfile;else{const{data:data,error:error}=await sb.from("profiles").select("*").eq("id",userId).single();if(error||!data)return void(container.innerHTML='<div class="view"><div class="empty-state">تعذر تحميل هذا المستخدم</div></div>');p=data}let isFollowing=!1;if(!isOwn){const{data:f}=await sb.from("user_follows").select("*").eq("follower_id",currentUser.id).eq("following_id",p.id).maybeSingle();isFollowing=!!f}const[{count:followers},{count:following}]=await Promise.all([sb.from("user_follows").select("*",{count:"exact",head:!0}).eq("following_id",p.id),sb.from("user_follows").select("*",{count:"exact",head:!0}).eq("follower_id",p.id)]),links=[p.github_url?`<a href="${escapeHtml(p.github_url)}" target="_blank"><i class="fa-brands fa-github"></i> GitHub</a>`:"",p.linkedin_url?`<a href="${escapeHtml(p.linkedin_url)}" target="_blank"><i class="fa-brands fa-linkedin"></i> LinkedIn</a>`:"",p.portfolio_url?`<a href="${escapeHtml(p.portfolio_url)}" target="_blank"><i class="fa-solid fa-briefcase"></i> Portfolio</a>`:"",p.website_url?`<a href="${escapeHtml(p.website_url)}" target="_blank"><i class="fa-solid fa-globe"></i> Website</a>`:""].filter(Boolean).join("");container.innerHTML=`\n    <div class="view">\n      <div class="profile-header">\n        <div class="profile-cover" id="profileCover" ${isOwn?"onclick=\"document.getElementById('coverUploadInput').click()\"":""}>\n          ${isOwn?'<span class="cover-hint"><i class="fa-solid fa-camera"></i> غيّر صورة الغلاف</span><input type="file" id="coverUploadInput" accept="image/*" style="display:none" onchange="uploadCover(this)">':""}\n        </div>\n        <div class="profile-body">\n          <div class="profile-top">\n            <div class="avatar-lg" id="profileAvatarLg" style="position:relative;cursor:${isOwn?"pointer":"default"}" ${isOwn?"onclick=\"document.getElementById('avatarUploadInput').click()\"":""}>${(p.username||"?")[0].toUpperCase()}</div>\n            <div>\n              <h2 style="font-family:var(--font-display);font-size:22px" id="profileDisplayName">${escapeHtml(p.full_name||p.username)}</h2>\n              <div style="color:var(--ink-faint);font-family:var(--font-mono);font-size:13px">@${p.username} · ${p.role}</div>\n            </div>\n            ${isOwn?'<input type="file" id="avatarUploadInput" accept="image/*" style="display:none" onchange="uploadAvatar(this)">':""}\n          </div>\n          <div style="color:var(--ink-dim);font-size:14px">${escapeHtml(p.bio||(isOwn?"مفيش نبذة لسه — اضغط تعديل عشان تضيف واحدة":""))}</div>\n          ${links?`<div class="social-links">${links}</div>`:""}\n          <div class="profile-stats">\n            <div><div class="n">${p.xp??0}</div><div class="l">XP</div></div>\n            <div><div class="n">${p.level??0}</div><div class="l">Level</div></div>\n            <div><div class="n">${p.reputation}</div><div class="l">Reputation</div></div>\n            <div><div class="n">${followers||0}</div><div class="l">متابِع</div></div>\n            <div><div class="n">${following||0}</div><div class="l">يتابع</div></div>\n          </div>\n          ${renderXpProgressBar(p.xp??0,p.level??0)}\n          <div style="margin-top:12px">${(p.skills||[]).map(s=>`<span class="tag">${escapeHtml(s)}</span>`).join("")||'<span style="color:var(--ink-faint);font-size:12px">مفيش مهارات مضافة</span>'}</div>\n          ${isOwn?'<button class="btn btn-sm" style="margin-top:16px" onclick="editProfile()">تعديل الملف الشخصي</button>\n               <button class="btn btn-sm" style="margin-top:16px" onclick="openLocalDisplayNamePrompt()"><i class="fa-solid fa-pen"></i> اسمي المحلي</button>\n               <button class="btn btn-sm" style="margin-top:16px" onclick="openLocalSettingsModal()"><i class="fa-solid fa-gear"></i> الإعدادات</button>':`<button class="btn btn-sm ${isFollowing?"":"btn-primary"}" style="margin-top:16px" onclick="toggleFollow('${p.id}', ${isFollowing})">${isFollowing?"إلغاء المتابعة":"متابعة"}</button>\n               <button class="btn btn-sm" style="margin-top:16px" onclick="openDmConversation('${p.id}','${escapeHtml(p.username)}')"><i class="fa-solid fa-envelope"></i> رسالة</button>\n               <button class="btn btn-sm" style="margin-top:16px" onclick="openNicknamePrompt('${p.id}','${escapeHtml(p.username).replace(/'/g,"\\'")}','${escapeHtml(p.full_name||p.username).replace(/'/g,"\\'")}')"><i class="fa-solid fa-tag"></i> اسم مستعار</button>`}\n        </div>\n      </div>\n      <div class="profile-tabs">\n        <div class="profile-tab active" data-ptab="posts" onclick="switchProfileTab('posts','${p.id}',${isOwn})">المنشورات</div>\n        <div class="profile-tab" data-ptab="portfolio" onclick="switchProfileTab('portfolio','${p.id}',${isOwn})">معرض المشاريع</div>\n        <div class="profile-tab" data-ptab="gallery" onclick="switchProfileTab('gallery','${p.id}',${isOwn})">معرض الصور</div>\n        <div class="profile-tab" data-ptab="media" onclick="switchProfileTab('media','${p.id}',${isOwn})">الوسائط</div>\n        <div class="profile-tab" data-ptab="activity" onclick="switchProfileTab('activity','${p.id}',${isOwn})">النشاط</div>\n      </div>\n      <div id="profileTabContent"><div class="empty-state"><div class="spinner"></div></div></div>\n    </div>`,p.avatar_file_id&&loadAvatarInto("profileAvatarLg",p.avatar_file_id),p.cover_file_id&&loadCoverInto("profileCover",p.cover_file_id),applyLocalNameToProfileHeader(p,isOwn),switchProfileTab("posts",p.id,isOwn)}async function applyLocalNameToProfileHeader(p,isOwn){const nameEl=document.getElementById("profileDisplayName");if(nameEl)if(isOwn){const localName=await getLocalDisplayName();localName&&(nameEl.textContent=localName)}else{const nick=await getNickname(p.id);nick&&(nameEl.textContent=nick)}}async function openLocalDisplayNamePrompt(){const current=await getLocalDisplayName(),name=await customPrompt("اسمك اللي هيظهرلك أنت بس في التطبيق (محلي على جهازك، مش هيتغير عند حد تاني):",current||currentProfile.full_name||currentProfile.username);if(null===name)return;await setLocalDisplayName(name.trim());const nameEl=document.getElementById("profileDisplayName");nameEl&&(nameEl.textContent=name.trim()||currentProfile.full_name||currentProfile.username),toast("اتحفظ اسمك المحلي","success")}async function openNicknamePrompt(userId,username,currentFullName){const current=await getNickname(userId),nickname=await customPrompt(`اسم مستعار لـ @${username} (يظهرلك أنت بس، محلي على جهازك):`,current||"");if(null===nickname)return;await setNickname(userId,nickname);const nameEl=document.getElementById("profileDisplayName");nameEl&&(nameEl.textContent=nickname.trim()||currentFullName),toast(nickname.trim()?"اتحفظ الاسم المستعار":"اتشال الاسم المستعار","success")}async function loadCoverInto(elId,fileId){const el=document.getElementById(elId);if(!el)return;const cached=getCachedFile(fileId);if(cached)return el.style.backgroundImage=`url(${cached})`,el.style.backgroundSize="cover",void(el.style.backgroundPosition="center");const url=await getTelegramFileBlobUrl(fileId);url&&(el.style.backgroundImage=`url(${url})`,el.style.backgroundSize="cover",el.style.backgroundPosition="center")}async function uploadCover(input){const file=input.files[0];if(file)try{const up=await uploadToTelegram(file,"photo",{type:"profile"}),{error:error}=await sb.from("profiles").update({cover_file_id:up.file_id}).eq("id",currentUser.id);if(error)throw error;currentProfile.cover_file_id=up.file_id,toast("تم تحديث صورة الغلاف","success"),loadCoverInto("profileCover",up.file_id)}catch(e){toast("فشل رفع صورة الغلاف: "+e.message,"error")}}async function switchProfileTab(tab,userId,isOwn){document.querySelectorAll(".profile-tab").forEach(t=>t.classList.toggle("active",t.dataset.ptab===tab));const el=document.getElementById("profileTabContent");if(el.innerHTML='<div class="empty-state"><div class="spinner"></div></div>',"posts"===tab){const{data:posts}=await sb.from("posts").select("*").eq("author_id",userId).eq("is_removed",!1).order("created_at",{ascending:!1});el.innerHTML=posts&&posts.length?posts.map((post,i)=>`\n      <div class="post-card" style="animation-delay:${.04*i}s"><div class="post-content">${escapeHtml(post.content||"")}</div><div class="meta" style="font-family:var(--font-mono);font-size:12px;color:var(--ink-faint)">${timeAgo(post.created_at)}</div></div>\n    `).join(""):`<div class="empty-state">${isOwn?"لسه معملتش أي منشورات":"مفيش منشورات لسه"}</div>`}else if("portfolio"===tab){const{data:items}=await sb.from("portfolio_items").select("*").eq("user_id",userId).order("sort_order");el.innerHTML=(isOwn?`<button class="btn btn-sm" style="margin-bottom:14px" onclick="addPortfolioItem('${userId}')">+ إضافة مشروع</button>`:"")+`<div class="portfolio-grid">${(items||[]).map((it,i)=>`\n        <div class="portfolio-card" style="animation:fadeInUp 0.4s var(--ease-out) ${.05*i}s both">\n          <h4>${escapeHtml(it.title)}</h4>\n          <p>${escapeHtml(it.description||"")}</p>\n          ${it.project_url?`<a href="${escapeHtml(it.project_url)}" target="_blank">عرض المشروع <i class="fa-solid fa-arrow-up-right-from-square"></i></a>`:""}\n          ${isOwn?`<div style="margin-top:8px"><span style="font-size:11px;color:var(--red);cursor:pointer" onclick="deletePortfolioItem('${it.id}','${userId}')">حذف</span></div>`:""}\n        </div>`).join("")||'<div class="empty-state">لسه مفيش مشاريع مضافة</div>'}</div>`}else if("gallery"===tab){const{data:items}=await sb.from("gallery_items").select("*").eq("user_id",userId).order("created_at",{ascending:!1});el.innerHTML=(isOwn?`<button class="btn btn-sm" style="margin-bottom:14px" onclick="document.getElementById('galleryUploadInput').click()">+ رفع صورة</button><input type="file" id="galleryUploadInput" accept="image/*" style="display:none" onchange="addGalleryItem(this,'${userId}')">`:"")+`<div class="gallery-grid" id="galleryGrid">${items&&items.length?items.map(it=>`<div id="gal-${it.id}"></div>`).join(""):'<div class="empty-state">لسه مفيش صور</div>'}</div>`,(items||[]).forEach(it=>loadGalleryThumb(it.id,it.image_file_id,isOwn))}else if("media"===tab){const[{data:postMedia},{data:galleryMedia}]=await Promise.all([sb.from("posts").select("id, post_type, telegram_file_id, created_at").eq("author_id",userId).eq("is_removed",!1).in("post_type",["image","video"]).not("telegram_file_id","is",null).order("created_at",{ascending:!1}),sb.from("gallery_items").select("id, image_file_id, created_at").eq("user_id",userId).order("created_at",{ascending:!1})]),combined=[...(postMedia||[]).map(p=>({key:"post-"+p.id,fileId:p.telegram_file_id,type:p.post_type,time:p.created_at,source:"post",refId:p.id})),...(galleryMedia||[]).map(g=>({key:"gal-"+g.id,fileId:g.image_file_id,type:"image",time:g.created_at,source:"gallery",refId:g.id}))].sort((a,b)=>new Date(b.time)-new Date(a.time));el.innerHTML=combined.length?`<div class="gallery-grid" id="mediaGrid">${combined.map(it=>`<div id="med-${it.key}"></div>`).join("")}</div>`:'<div class="empty-state">لسه مفيش وسائط</div>',combined.forEach(it=>loadProfileMediaThumb(it))}else if("activity"===tab){const[{data:posts},{data:articles}]=await Promise.all([sb.from("posts").select("id, content, created_at").eq("author_id",userId).eq("is_removed",!1).order("created_at",{ascending:!1}).limit(15),sb.from("articles").select("id, title, created_at").eq("author_id",userId).eq("is_draft",!1).order("created_at",{ascending:!1}).limit(15)]),events=[...(posts||[]).map(p=>({type:"post",time:p.created_at,text:"نشر: "+(p.content||"").slice(0,60)})),...(articles||[]).map(a=>({type:"article",time:a.created_at,text:"كتب مقال: "+a.title}))].sort((a,b)=>new Date(b.time)-new Date(a.time));el.innerHTML=events.length?events.map((e,i)=>`\n      <div style="display:flex;gap:10px;padding:10px 0;border-bottom:1px solid var(--border);font-size:13px;animation:fadeIn 0.3s var(--ease) ${.04*i}s both">\n        <span>${"post"===e.type?'<i class="fa-solid fa-pen-to-square"></i>':'<i class="fa-solid fa-newspaper"></i>'}</span><div style="flex:1">${escapeHtml(e.text)}</div><span style="color:var(--ink-faint);font-size:11px;font-family:var(--font-mono)">${timeAgo(e.time)}</span>\n      </div>`).join(""):'<div class="empty-state">لسه مفيش نشاط</div>'}}async function addPortfolioItem(userId){const title=await customPrompt("اسم المشروع:");if(!title)return;const description=await customPrompt("وصف قصير:")||"",project_url=await customPrompt("رابط المشروع (اختياري):")||null,{error:error}=await sb.from("portfolio_items").insert({user_id:currentUser.id,title:title,description:description,project_url:project_url});error?toast("خطأ: "+error.message,"error"):(toast("تم إضافة المشروع","success"),switchProfileTab("portfolio",userId,!0))}async function deletePortfolioItem(itemId,userId){await customConfirm("تحذف المشروع ده؟",!0)&&(await sb.from("portfolio_items").delete().eq("id",itemId),switchProfileTab("portfolio",userId,!0))}async function addGalleryItem(input,userId){const file=input.files[0];if(file)try{const up=await uploadToTelegram(file,"photo",{type:"profile"}),{error:error}=await sb.from("gallery_items").insert({user_id:currentUser.id,image_file_id:up.file_id,telegram_message_id:up.telegram_message_id||null});if(error)throw error;toast("تمت الإضافة","success"),switchProfileTab("gallery",userId,!0)}catch(e){toast("فشل الرفع: "+e.message,"error")}}async function loadGalleryThumb(itemId,fileId,isOwn){const url=getCachedFile(fileId)||await getTelegramFileBlobUrl(fileId);url&&getCachedFile(fileId);const holder=document.getElementById(`gal-${itemId}`);holder&&url&&(holder.innerHTML=`<img class="gallery-thumb" src="${url}">${isOwn?`<div style="text-align:center;font-size:11px;color:var(--red);cursor:pointer;margin-top:2px" onclick="deleteGalleryItem('${itemId}')">حذف</div>`:""}`)}async function deleteGalleryItem(itemId){await customConfirm("تحذف الصورة دي؟",!0)&&(await sb.from("gallery_items").delete().eq("id",itemId),document.getElementById(`gal-${itemId}`)?.parentElement?.removeChild(document.getElementById(`gal-${itemId}`)))}async function loadProfileMediaThumb(it){const holder=document.getElementById(`med-${it.key}`);if(!holder)return;if("video"===it.type)return void(holder.innerHTML=`<div class="video-play-placeholder gallery-thumb" onclick="openProfileMediaVideo('${it.key}','${escapeHtml(it.fileId)}')"><i class="fa-solid fa-circle-play"></i></div>`);const url=getCachedFile(it.fileId)||await getTelegramFileBlobUrl(it.fileId);url&&(holder.innerHTML=`<img class="gallery-thumb" src="${url}" onclick="window.open('${url}','_blank')">`)}async function openProfileMediaVideo(key,fileId){const holder=document.getElementById(`med-${key}`);if(!holder)return;const streamUrl=await getTelegramStreamUrl(fileId);streamUrl?holder.innerHTML=`<video src="${streamUrl}" controls autoplay playsinline class="gallery-thumb"></video>`:toast("تعذر تشغيل الفيديو","error")}async function uploadAvatar(input){const file=input.files[0];if(file)try{const up=await uploadToTelegram(file,"photo",{type:"profile"}),{error:error}=await sb.from("profiles").update({avatar_file_id:up.file_id}).eq("id",currentUser.id);if(error)throw error;currentProfile.avatar_file_id=up.file_id,toast("تم تحديث الصورة الشخصية","success"),loadAvatarInto("profileAvatarLg",up.file_id),loadAvatarInto("miniAvatar",up.file_id)}catch(e){toast("فشل رفع الصورة: "+e.message,"error")}}async function toggleFollow(targetId,currentlyFollowing){currentlyFollowing?await sb.from("user_follows").delete().eq("follower_id",currentUser.id).eq("following_id",targetId):await sb.from("user_follows").insert({follower_id:currentUser.id,following_id:targetId}),renderProfile(document.getElementById("viewContainer"),targetId)}